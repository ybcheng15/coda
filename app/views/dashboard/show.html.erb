<div class="max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6">Welcome, <%= current_user.name %></h1>

    <div class="bg-white rounded-lg shadow p-6 mb-6">
      <h2 class="text-xl font-semibold mb-4">Your Spotify Stats</h2>
      <p class="text-gray-600">We'll display your stats here soon!</p>
    </div>

    <div class="text-center">
        <%= button_to "Logout", logout_path, method: :delete, class: "bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded" %>
    </div>
</div>